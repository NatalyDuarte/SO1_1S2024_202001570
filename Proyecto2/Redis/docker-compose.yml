version: '3.8'
services:
  redis:
    container_name: redis-persistent  # More descriptive name
    image: redis:6.2-alpine
    restart: always
    ports:
      - '6379:6379'
    volumes:
      - name : redis-data # Mount the local volume
    command: redis-server --loglevel warning --requirepass proyectnaty
